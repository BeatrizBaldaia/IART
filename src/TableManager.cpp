#include "Person.h"
#include "Table.h"
#include <map>

/**
 * argv[1] = nome do ficheiro de pessoas
 * argv[2] = nome do ficheiro de de mesas
 */
int main(int argc, const char * argv[]) {

	vector<Person> people;
	vector<Table> tables;
	vector<int> solution;//cada indice equivale a uma pessoa e o valor do mesmo equivale a mesa onde ela se vai sentar
	return 0;
}
